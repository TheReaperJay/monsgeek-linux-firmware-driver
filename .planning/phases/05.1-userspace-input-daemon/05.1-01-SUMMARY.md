---
phase: 05.1-userspace-input-daemon
plan: 01
subsystem: transport, input-daemon
tags: [usb, uinput, evdev, clap, toml, session-mode]

requires:
  - phase: 02-transport
    provides: USB session with IF0/IF1/IF2 claiming, InputProcessor, keymap
provides:
  - SessionMode::InputOnly variant (IF0/IF1 only, leaves IF2 free)
  - monsgeek-inputd binary crate scaffold
  - Config loading with CLI > file > hardcoded 15ms fallback
  - uinput virtual device creation and KeyAction-to-InputEvent conversion
affects: [05.1-02-PLAN, 05.1-03-PLAN, grpc-bridge]

tech-stack:
  added: [evdev 0.13, sd-notify 0.5, clap 4.6, signal-hook 0.3, toml 1.1]
  patterns: [session-mode-dispatch, config-priority-chain]

key-files:
  created:
    - crates/monsgeek-inputd/Cargo.toml
    - crates/monsgeek-inputd/src/main.rs
    - crates/monsgeek-inputd/src/config.rs
    - crates/monsgeek-inputd/src/uinput_device.rs
  modified:
    - crates/monsgeek-transport/src/usb.rs
    - crates/monsgeek-transport/src/lib.rs

key-decisions:
  - "SessionMode::InputOnly uses match-on-mode dispatch instead of boolean flags for interface selection"
  - "evdev KeyCode (not Key) is the correct type in evdev 0.13 API"
  - "InputEvent::new takes raw u16 for event type, requiring EventType::KEY.0"

patterns-established:
  - "Match-on-mode: all interface/behavior dispatch uses match on SessionMode, not boolean flags"
  - "Config priority chain: CLI flag > user config > system config > hardcoded default"

requirements-completed: [INPUT-04, INPUT-01]

duration: 5min
completed: 2026-03-26
---

# Phase 05.1 Plan 01: Transport InputOnly Mode + Daemon Scaffold Summary

**SessionMode::InputOnly for IF0/IF1-only claiming plus monsgeek-inputd crate with tested config loading and uinput KeyAction conversion**

## Performance

- **Duration:** 5 min
- **Started:** 2026-03-26T15:37:30Z
- **Completed:** 2026-03-26T15:42:40Z
- **Tasks:** 2
- **Files modified:** 6

## Accomplishments
- Added SessionMode::InputOnly to transport layer that claims IF0/IF1 but leaves IF2 free for gRPC bridge coexistence
- Scaffolded monsgeek-inputd binary crate with config, uinput_device, and CLI modules
- Config resolution follows strict priority: CLI flag > user TOML > system TOML > hardcoded 15ms
- KeyAction-to-InputEvent conversion produces correct EV_KEY events via evdev 0.13

## Task Commits

Each task was committed atomically:

1. **Task 1: Add SessionMode::InputOnly to transport layer** - `808ab8e` (feat)
2. **Task 2: Scaffold monsgeek-inputd crate with config and uinput modules** - `b957bce` (feat)

## Files Created/Modified
- `crates/monsgeek-transport/src/usb.rs` - Added InputOnly variant, updated open_impl and Drop interface dispatch
- `crates/monsgeek-transport/src/lib.rs` - Added InputOnly to spawn_transport match, added input_only() constructor
- `crates/monsgeek-inputd/Cargo.toml` - Binary crate with evdev, clap, sd-notify, signal-hook dependencies
- `crates/monsgeek-inputd/src/main.rs` - CLI parser with --debounce-ms and --device flags
- `crates/monsgeek-inputd/src/config.rs` - TOML config loading with fallback hierarchy
- `crates/monsgeek-inputd/src/uinput_device.rs` - uinput VirtualDevice creation and KeyAction conversion

## Decisions Made
- Used match-on-mode dispatch throughout usb.rs instead of boolean `claims_input` flags -- clearer semantics, exhaustive match catches future variants
- evdev 0.13 uses `KeyCode` (not `Key`) and `InputEvent::new(u16, u16, i32)` requiring `EventType::KEY.0` for the type field
- Config resolution returns the first non-None value in CLI > file > default chain rather than merging configs

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Fixed evdev API types**
- **Found during:** Task 2 (uinput_device module)
- **Issue:** Plan specified `Key` and `InputEvent::new(EventType::KEY, ...)` but evdev 0.13 uses `KeyCode` and requires `EventType::KEY.0` (raw u16)
- **Fix:** Changed imports to `KeyCode`, used `EventType::KEY.0` in InputEvent constructor
- **Files modified:** crates/monsgeek-inputd/src/uinput_device.rs
- **Verification:** cargo build and all tests pass
- **Committed in:** b957bce (Task 2 commit)

---

**Total deviations:** 1 auto-fixed (1 blocking)
**Impact on plan:** API mismatch was a straightforward type correction. No scope creep.

## Issues Encountered
None beyond the evdev API type correction noted in deviations.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- SessionMode::InputOnly and daemon scaffold are ready for Plan 02 (daemon event loop)
- InputProcessor, keymap, and uinput modules provide all building blocks for the pump loop
- Config loading and CLI parsing are complete for daemon startup

---
*Phase: 05.1-userspace-input-daemon*
*Completed: 2026-03-26*
