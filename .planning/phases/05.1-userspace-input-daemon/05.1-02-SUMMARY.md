---
phase: 05.1-userspace-input-daemon
plan: 02
subsystem: input-daemon
tags: [uinput, usb, hid, signal-hook, udev, sd-notify, daemon]

requires:
  - phase: 05.1-01
    provides: SessionMode::InputOnly, monsgeek-inputd scaffold, config, uinput_device module
provides:
  - Daemon main loop with connect/poll/disconnect/reconnect lifecycle
  - Signal handling (SIGTERM/SIGINT) with clean key release on shutdown
  - udev-based automatic reconnect on keyboard disconnect
  - sd_notify integration for systemd readiness, status, and watchdog
affects: [05.1-03-PLAN, systemd-integration]

tech-stack:
  added: [udev 0.9.3 (inputd), libc 0.2.183 (inputd)]
  patterns: [daemon-lifecycle-loop, fresh-processor-per-connect, udev-reconnect]

key-files:
  created:
    - crates/monsgeek-inputd/src/daemon.rs
  modified:
    - crates/monsgeek-inputd/src/main.rs
    - crates/monsgeek-inputd/Cargo.toml

key-decisions:
  - "sd-notify 0.5 API takes &[NotifyState] without a bool first argument (plan referenced older API)"
  - "Device registry loaded from cwd-relative path with exe-relative fallback"
  - "500ms settle delay after udev reconnect event before attempting USB open"

patterns-established:
  - "Fresh InputProcessor per connect: clears stale debounce state on reconnect"
  - "Daemon lifecycle: outer connect/reconnect loop wrapping inner poll loop"
  - "udev reconnect: MonitorBuilder with poll(fd, 1s) and VID-based PRODUCT filter"

requirements-completed: [INPUT-01, INPUT-02, INPUT-03]

duration: 4min
completed: 2026-03-26
---

# Phase 05.1 Plan 02: Daemon Event Loop Summary

**Full daemon lifecycle with IF0 polling via InputOnly session, InputProcessor debounce, uinput emission, SIGTERM/SIGINT shutdown with key release, and udev-based auto-reconnect**

## Performance

- **Duration:** 4 min
- **Started:** 2026-03-26T15:45:33Z
- **Completed:** 2026-03-26T15:49:37Z
- **Tasks:** 2
- **Files modified:** 3

## Accomplishments
- Implemented complete daemon loop: device discovery, IF0 polling, InputProcessor report processing, uinput event emission
- Signal handling via signal_hook with clean release_all_keys on both disconnect and SIGTERM/SIGINT paths
- Automatic keyboard reconnect via udev MonitorBuilder monitoring with VID-based filtering
- sd_notify lifecycle notifications: Ready on uinput creation, Status on connect/disconnect, Watchdog every 5s

## Task Commits

Each task was committed atomically:

1. **Task 1: Implement daemon loop with lifecycle management** - `5e8bc9e` (feat)
2. **Task 2: Wire daemon loop into main.rs entry point** - `3776c82` (feat)

## Files Created/Modified
- `crates/monsgeek-inputd/src/daemon.rs` - Full daemon lifecycle: connect, poll, disconnect, reconnect, shutdown
- `crates/monsgeek-inputd/src/main.rs` - Entry point wiring CLI, config, device filter parsing, and daemon loop
- `crates/monsgeek-inputd/Cargo.toml` - Added udev and libc dependencies for reconnect monitoring

## Decisions Made
- sd-notify 0.5 API changed from the 0.4 API referenced in the plan: `notify(&[...])` instead of `notify(false, &[...])`. Fixed inline.
- Removed `DaemonError::Registry` variant since registry errors flow through `Box<dyn Error>` from `load_registry()` and `find_target_device()`.
- Device registry path resolution tries CWD-relative first, exe-relative second, supporting both dev and installed scenarios.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Fixed sd_notify::notify API signature for 0.5**
- **Found during:** Task 2 (build verification after wiring main.rs)
- **Issue:** Plan specified `sd_notify::notify(false, &[...])` but sd-notify 0.5 takes `notify(&[...])` without the bool argument
- **Fix:** Removed `false` first argument from all 4 sd_notify::notify calls
- **Files modified:** crates/monsgeek-inputd/src/daemon.rs
- **Verification:** cargo build -p monsgeek-inputd succeeds
- **Committed in:** 3776c82 (Task 2 commit)

---

**Total deviations:** 1 auto-fixed (1 blocking)
**Impact on plan:** API mismatch was a straightforward signature correction. No scope creep.

## Issues Encountered
None beyond the sd-notify API correction noted in deviations.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Daemon binary is fully functional (pending hardware verification in Plan 03)
- All lifecycle paths implemented: startup, connected polling, disconnect handling, reconnect via udev, clean shutdown
- Binary shows correct --help with --debounce-ms and --device flags
- No regressions in workspace tests

---
*Phase: 05.1-userspace-input-daemon*
*Completed: 2026-03-26*
