---
phase: 05.1-userspace-input-daemon
verified: 2026-03-27T00:00:00Z
status: passed
score: 17/17 must-haves verified
gaps: []
human_verification:
  - test: "Run the daemon on a connected M5W and verify typing, debounce, bridge coexistence, shutdown, and disconnect/reconnect"
    expected: "All 7 steps from Plan 03 Task 2 pass: uinput device visible, typing works, debounce active, bridge coexists, clean shutdown, reconnect works"
    why_human: "Hardware I/O path cannot be verified programmatically — requires physical keyboard, uinput device observation, and subjective debounce feel"
    status: "APPROVED — user confirmed all 7 steps passed during Plan 03 checkpoint"
---

# Phase 05.1: Userspace Input Daemon Verification Report

**Phase Goal:** Userspace input daemon — standalone binary that claims IF0/IF1, processes HID boot protocol reports through InputProcessor with software debounce, and injects clean key events via uinput. Coexists with gRPC bridge (daemon on IF0, bridge on IF2).
**Verified:** 2026-03-27
**Status:** PASSED
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| #  | Truth                                                                                        | Status     | Evidence |
|----|----------------------------------------------------------------------------------------------|------------|----------|
| 1  | SessionMode::InputOnly claims IF0/IF1 but NOT IF2                                            | VERIFIED   | `usb.rs` line 212: `SessionMode::InputOnly => &[IF0, IF1]` in `open_impl` interfaces match; Drop impl line 472 same. IF2 never in this branch. |
| 2  | InputOnly mode sends SET_PROTOCOL(Boot) on IF0                                               | VERIFIED   | `open_impl` line 181: `claims_input = matches!(mode, UserspaceInput | InputOnly)` — SET_PROTOCOL block at line 250 gates on `claims_input`. |
| 3  | InputOnly Drop releases IF0/IF1 and reattaches kernel driver, does NOT touch IF2             | VERIFIED   | `Drop` impl lines 469–491: match arm `InputOnly => &[IF0, IF1]`; reattaches IF0 if `detached_if0`; only reattaches IF2 if `detached_if2` (which is never set for InputOnly). |
| 4  | monsgeek-inputd crate compiles as a binary with all required dependencies                    | VERIFIED   | `Cargo.toml` contains: `evdev = "0.13"`, `sd-notify = "0.5"`, `clap = "4.6"`, `toml = "1.1"`, `signal-hook = "0.3"`, `udev = "0.9.3"`, `libc = "0.2.183"`. `cargo test --workspace` exits 0. |
| 5  | Config loading respects priority: CLI flag > user config > system config > hardcoded 15ms   | VERIFIED   | `config.rs` lines 46–48: `cli_flag.or(config.debounce_ms).unwrap_or(DEFAULT_DEBOUNCE_MS)`. `DEFAULT_DEBOUNCE_MS = 15`. Three unit tests confirm all three branches pass. |
| 6  | uinput virtual device registers all keycodes from all_keycodes()                             | VERIFIED   | `uinput_device.rs` lines 9–11: iterates `all_keycodes()` and inserts each `KeyCode::new(keycode)` into `AttributeSet`. |
| 7  | KeyAction to evdev InputEvent conversion produces correct EV_KEY events                      | VERIFIED   | `uinput_device.rs` line 22: `InputEvent::new(EventType::KEY.0, action.keycode, action.value)`. Three unit tests verify press/release/batch conversions. |
| 8  | Daemon opens USB session in InputOnly mode, polls IF0, processes through InputProcessor, emits via uinput | VERIFIED | `daemon.rs` lines 158–162: `UsbSession::open_at_with_mode(bus, addr, SessionMode::InputOnly)` / `open_with_mode(vid, pid, SessionMode::InputOnly)`. Lines 165/167: `InputProcessor::new` and `create_uinput_device`. Lines 188–197: poll loop calling `process_report` then `emit_actions`. |
| 9  | Daemon handles SIGTERM/SIGINT by releasing all held keys and destroying uinput device        | VERIFIED   | `daemon.rs` lines 86–87: `signal_hook::flag::register(SIGTERM/SIGINT, shutdown)`. Lines 232–234: clean shutdown path calls `release_all_keys` then drops `uinput_dev` and `session`. |
| 10 | Daemon survives keyboard disconnect and reconnects automatically via udev monitoring         | VERIFIED   | `daemon.rs` lines 109–136: outer reconnect loop; on `Disconnected`, calls `wait_for_device_udev` (lines 240–295) which uses `udev::MonitorBuilder` with VID-based `PRODUCT` filter and `libc::poll`. |
| 11 | Daemon sends sd_notify READY=1 after uinput device is created                               | VERIFIED   | `daemon.rs` line 169: `sd_notify::notify(&[sd_notify::NotifyState::Ready])` called immediately after `create_uinput_device` returns. |
| 12 | Daemon sends sd_notify STATUS updates and periodic Watchdog notifications                   | VERIFIED   | Lines 170–173: `Status(Connected...)`. Lines 117–121: `Status(Waiting...)`. Lines 183–185: Watchdog every 5s via `WATCHDOG_INTERVAL`. |
| 13 | Fresh InputProcessor created on each reconnect to clear stale debounce state                 | VERIFIED   | `daemon.rs` line 165: `InputProcessor::new(config.debounce_ms)` is inside `try_connect_and_run`, which is called on every reconnect iteration. |
| 14 | main.rs wires CLI, config, device filter, and daemon loop                                    | VERIFIED   | `main.rs` lines 26–46: `--device` parsing to `(bus, addr)`. Line 48: `DaemonConfig` construction. Line 54: `daemon::run_daemon(daemon_config)`. |
| 15 | Hardware integration tests verify InputOnly, uinput, and coexistence                         | VERIFIED   | `tests/hardware.rs` line 1: `#![cfg(feature = "hardware")]`. Tests: `test_input_only_session_opens`, `test_uinput_device_creation`, `test_daemon_coexistence`, `test_input_poll_produces_actions`. |
| 16 | find_devices_no_probe() prevents bus disruption during daemon discovery                      | VERIFIED   | `discovery.rs` lines 61–99: `find_devices_no_probe` reads descriptors only, never opens sessions or claims interfaces. `daemon.rs` line 332 uses it. |
| 17 | Consecutive error threshold detects disconnect when kernel returns ambiguous errors          | VERIFIED   | `daemon.rs` lines 69–72: `DISCONNECT_ERROR_THRESHOLD = 10`. Lines 212–224: increments counter and treats as `Disconnected` at threshold. |

**Score:** 17/17 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `crates/monsgeek-transport/src/usb.rs` | SessionMode::InputOnly variant | VERIFIED | Line 54: `InputOnly,` in enum. open_impl and Drop both dispatch correctly on it. |
| `crates/monsgeek-inputd/Cargo.toml` | Binary crate with daemon dependencies | VERIFIED | Contains `name = "monsgeek-inputd"`, `evdev = "0.13"`, all plan-specified deps present. |
| `crates/monsgeek-inputd/src/config.rs` | TOML config loading with fallback hierarchy | VERIFIED | Contains `fn load_config`, `fn resolve_debounce_ms`, `const DEFAULT_DEBOUNCE_MS: u64 = 15`. 6 unit tests. |
| `crates/monsgeek-inputd/src/uinput_device.rs` | uinput VirtualDevice creation and event emission | VERIFIED | Contains `fn create_uinput_device`, `fn emit_actions`, `fn key_action_to_input_event`. Imports `all_keycodes` and `KeyAction`. |
| `crates/monsgeek-inputd/src/daemon.rs` | Main daemon loop with full lifecycle | VERIFIED | Contains `pub fn run_daemon`, all signal handling, udev reconnect, sd_notify, poll loop with `release_all_keys` on both disconnect and shutdown paths. |
| `crates/monsgeek-inputd/src/main.rs` | Entry point wiring CLI and daemon loop | VERIFIED | Contains `mod daemon;`, `daemon::run_daemon(daemon_config)`, `device_filter` parsing, `"MonsGeek M5W (monsgeek-inputd)"` device name. |
| `crates/monsgeek-inputd/src/lib.rs` | Library root exposing uinput_device for tests | VERIFIED | `pub mod uinput_device;` — enables `monsgeek_inputd::uinput_device::create_uinput_device` import in hardware tests. |
| `crates/monsgeek-inputd/tests/hardware.rs` | Hardware integration tests | VERIFIED | Contains all 4 required test functions, `#![cfg(feature = "hardware")]`, `fn test_daemon_coexistence`, `SessionMode::InputOnly`. |
| `crates/monsgeek-transport/src/discovery.rs` | find_devices_no_probe for non-invasive discovery | VERIFIED | Lines 61–99: full implementation present, never opens sessions. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `usb.rs` | `SessionMode` enum | `InputOnly` variant added | VERIFIED | Enum line 54 contains `InputOnly,`; dispatch in open_impl line 212 and Drop line 469. |
| `uinput_device.rs` | `monsgeek_transport::keymap::all_keycodes` | import for key registration | VERIFIED | Line 4: `use monsgeek_transport::keymap::all_keycodes;` — used in `create_uinput_device` line 10. |
| `uinput_device.rs` | `monsgeek_transport::input::KeyAction` | import for event conversion | VERIFIED | Line 3: `use monsgeek_transport::input::KeyAction;` — used in `key_action_to_input_event` signature. |
| `daemon.rs` | `UsbSession` with `SessionMode::InputOnly` | `open_at_with_mode`/`open_with_mode` | VERIFIED | Lines 159/161: both `open_at_with_mode(bus, addr, SessionMode::InputOnly)` and `open_with_mode(vid, pid, SessionMode::InputOnly)`. |
| `daemon.rs` | `InputProcessor::new` | fresh instance per connect | VERIFIED | Line 165: `let mut processor = InputProcessor::new(config.debounce_ms);` — inside `try_connect_and_run`, called fresh on every reconnect. |
| `daemon.rs` | `create_uinput_device` and `emit_actions` | event injection | VERIFIED | Line 167: `create_uinput_device(&config.device_name)`. Line 193: `emit_actions(&mut uinput_dev, &actions)`. |
| `daemon.rs` | `signal_hook` | AtomicBool shutdown flag for SIGTERM/SIGINT | VERIFIED | Lines 85–87: `shutdown` Arc, `flag::register(SIGTERM/SIGINT)`. Lines 109/181: `shutdown.load(Ordering::Relaxed)` checks in both outer and inner loops. |
| `lib.rs` | `TransportMode::InputOnly` in spawn_transport match | InputOnly creates InputProcessor | VERIFIED | `lib.rs` line 274: `TransportMode::UserspaceInput | TransportMode::InputOnly => Some(InputProcessor::new(...))`. |
| `main.rs` | `daemon::run_daemon` | entry point calls daemon | VERIFIED | Line 2: `mod daemon;`. Line 54: `daemon::run_daemon(daemon_config)`. |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| INPUT-01 | 01, 02, 03 | Persistent daemon claims IF0, reads HID boot protocol, applies software debounce, injects via uinput | SATISFIED | SessionMode::InputOnly claims IF0/IF1, SET_PROTOCOL(Boot) sent, InputProcessor.process_report with debounce_ms, emit_actions via evdev VirtualDevice. Human verification approved. |
| INPUT-02 | 02, 03 | Daemon corrects same-report key ordering (releases before presses, deterministic ordering) | SATISFIED | `input.rs` line 19: "Emits releases before presses for correct rollover handling". `process_report_at` processes modifier releases before presses. Human verification confirmed. |
| INPUT-03 | 02, 03 | Daemon runs as separate binary from gRPC bridge, independent lifecycle | SATISFIED | `monsgeek-inputd` is a distinct binary crate. `daemon::run_daemon` uses `SessionMode::InputOnly`, never touches IF2. Does not import or depend on gRPC bridge crate. Human verification: both ran simultaneously. |
| INPUT-04 | 01, 03 | Daemon coexists with gRPC bridge (bridge on IF2, daemon on IF0) | SATISFIED | `SessionMode::InputOnly` leaves IF2 fully available. `test_daemon_coexistence` hardware test opens InputOnly + ControlOnly sessions simultaneously. Human verification step 5 confirmed. |

All 4 requirements (INPUT-01 through INPUT-04) are satisfied. REQUIREMENTS.md traceability table maps all four to Phase 5.1 with status Complete — consistent with verification findings.

### Anti-Patterns Found

No anti-patterns found. Scanned all created/modified files:
- No TODO/FIXME/PLACEHOLDER/HACK comments in any source file
- No empty implementations or return null/return {}/return [] stubs
- No console.log-only handlers
- Daemon loop comments are architectural ("This module does NOT use the transport thread") not stub indicators
- The `eprintln!("daemon loop not yet implemented")` placeholder from Plan 01 main.rs was fully replaced in Plan 02

### Human Verification Required

**Status: APPROVED by user during Plan 03 hardware checkpoint**

All 7 verification steps from Plan 03 Task 2 passed:

1. Daemon starts and logs "Connected" — approved
2. uinput device visible in `/proc/bus/input/devices` as "MonsGeek M5W (monsgeek-inputd)" — approved
3. All keys work correctly through the daemon — approved
4. Software debounce active and effective — approved
5. Daemon and gRPC bridge coexist simultaneously (IF0 + IF2) — approved
6. Clean shutdown on Ctrl+C — approved
7. Disconnect/reconnect cycle works without daemon restart — approved

### Build Verification

`cargo test --workspace` — all test suites pass:
- monsgeek-protocol: 29 tests pass
- monsgeek-transport: 93 + 80 unit tests pass
- monsgeek-inputd: 6 unit tests pass (config + uinput_device modules)
- No compilation errors, no warnings elevated to errors

### Phase Goal Assessment

The phase goal is fully achieved:

1. **Standalone binary** — `monsgeek-inputd` is a separate Cargo binary crate, independent of the gRPC bridge
2. **Claims IF0/IF1** — `SessionMode::InputOnly` claims IF0 and IF1, never IF2, by construction in all dispatch paths
3. **Processes HID boot protocol reports** — `SET_PROTOCOL(Boot)` sent on open, `InputProcessor::process_report` handles 8-byte boot protocol format
4. **Software debounce** — `InputProcessor` with configurable `debounce_ms` (default 15ms) via CLI/config/hardcoded fallback chain
5. **Injects via uinput** — `evdev::uinput::VirtualDeviceBuilder` with all keycodes from `all_keycodes()`, `emit_actions` sends `EV_KEY` events
6. **Coexists with gRPC bridge** — IF2 never touched by daemon; hardware-verified simultaneous IF0+IF2 operation

---

_Verified: 2026-03-27_
_Verifier: Claude (gsd-verifier)_
