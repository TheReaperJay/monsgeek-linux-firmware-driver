---
phase: 03-grpc-web-bridge
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - crates/monsgeek-driver/src/service/mod.rs
  - crates/monsgeek-driver/src/service/device_registry.rs
  - crates/monsgeek-driver/src/service/db_store.rs
  - crates/monsgeek-driver/src/bridge_transport.rs
  - crates/monsgeek-driver/src/main.rs
  - crates/monsgeek-transport/src/lib.rs
  - crates/monsgeek-transport/src/thread.rs
autonomous: true
requirements: [GRPC-02, GRPC-03, GRPC-04, GRPC-05, GRPC-06, GRPC-08]

must_haves:
  truths:
    - "Device identity exposed to web app uses synthetic bridge paths, not raw bus/address"
    - "watchDevList emits Init first, then Add/Remove deltas only"
    - "Both raw and checksum-aware message families are first-class and split send/read"
    - "DB compatibility for Phase 3 is in-memory and session-scoped"
  artifacts:
    - path: "crates/monsgeek-driver/src/service/device_registry.rs"
      provides: "Synthetic path map and reverse runtime lookup"
      contains: "make_path"
    - path: "crates/monsgeek-driver/src/service/mod.rs"
      provides: "Real implementations for watch/send/read/version/db MVP RPCs"
      contains: "async fn watch_dev_list"
    - path: "crates/monsgeek-driver/src/bridge_transport.rs"
      provides: "Shared split send/read adapter used by raw and msg families"
      contains: "send_command"
  key_links:
    - from: "crates/monsgeek-driver/src/service/mod.rs"
      to: "references/monsgeek-akko-linux/iot_driver_linux/src/grpc.rs"
      via: "watch list and split send/read semantics"
      pattern: "watch_dev_list"
    - from: "crates/monsgeek-driver/src/service/mod.rs"
      to: "crates/monsgeek-transport/src/thread.rs"
      via: "map transport lifecycle events to app-facing stream deltas"
      pattern: "TransportEvent"
---

<objective>
Implement the real Phase 3 bridge behavior: device stream semantics, split send/read RPCs, version and DB RPCs, and synthetic path/session mapping.

Purpose:
- make the server operational for configurator command flow
- align bridge semantics with working reference behavior
- preserve transport safety by using control-only mode and serialized transport paths

Output: operational DriverService for MVP RPCs with in-memory compatibility store.
</objective>

<context>
@.planning/phases/03-grpc-web-bridge/03-CONTEXT.md
@.planning/phases/03-grpc-web-bridge/03-RESEARCH.md
@references/monsgeek-akko-linux/iot_driver_linux/src/grpc.rs
@crates/monsgeek-transport/src/lib.rs
@crates/monsgeek-transport/src/thread.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add bridge-owned synthetic path registry and session map</name>
  <files>
    crates/monsgeek-driver/src/service/device_registry.rs
    crates/monsgeek-driver/src/service/mod.rs
  </files>
  <read_first>
    crates/monsgeek-driver/src/service/mod.rs
    references/monsgeek-akko-linux/iot_driver_linux/src/grpc.rs
    crates/monsgeek-transport/src/lib.rs
    crates/monsgeek-transport/src/thread.rs
    .planning/phases/03-grpc-web-bridge/03-CONTEXT.md
  </read_first>
  <action>
    Create a bridge-local registry that:
    1. derives synthetic compatibility path strings per discovered device
    2. stores mapping path -> runtime session/device metadata
    3. stores reverse mapping bus/address -> path for remove events
    4. keeps firmware device ID as canonical identity and path as runtime locator
    Wire the registry into DriverService so `open_device` resolves by synthetic path first and falls back by vid/pid only when safe.
    Decision traceability: implements D-03, D-04, D-05, D-06.
  </action>
  <verify>
    <automated>cd /home/whitebehemoth/Dev/Projects/monsgeek-firmware-driver && cargo test -p monsgeek-driver synthetic_path -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `crates/monsgeek-driver/src/service/device_registry.rs` contains path creation using a synthetic format with uniqueness suffix
    - `crates/monsgeek-driver/src/service/mod.rs` contains path -> session lookup before fallback path
    - `crates/monsgeek-driver/src/service/mod.rs` contains reverse mapping usage for remove handling
    - `cargo test -p monsgeek-driver synthetic_path -- --nocapture` exits 0
  </acceptance_criteria>
  <done>
    - Device sessions can be resolved by synthetic compatibility path
    - Remove events can identify prior announced device entries
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement watchDevList Init/Add/Remove stream from real transport events</name>
  <files>
    crates/monsgeek-driver/src/service/mod.rs
    crates/monsgeek-driver/src/main.rs
  </files>
  <read_first>
    crates/monsgeek-driver/src/service/mod.rs
    crates/monsgeek-driver/src/main.rs
    references/monsgeek-akko-linux/iot_driver_linux/src/grpc.rs
    crates/monsgeek-transport/src/thread.rs
    .planning/phases/03-grpc-web-bridge/03-CONTEXT.md
  </read_first>
  <action>
    Implement `watch_dev_list` to send one `Init` snapshot immediately and then stream broadcast events.
    Map only add/remove lifecycle changes to `DeviceListChangeType::Add` and `DeviceListChangeType::Remove`.
    Do not emit `Change` in Phase 3.
    Ensure service startup path initializes watcher plumbing so connect/disconnect transitions are published.
    Decision traceability: implements D-07, D-08, D-09.
  </action>
  <verify>
    <automated>cd /home/whitebehemoth/Dev/Projects/monsgeek-firmware-driver && cargo test -p monsgeek-driver watch_dev_list -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `crates/monsgeek-driver/src/service/mod.rs` contains `DeviceListChangeType::Init`
    - `crates/monsgeek-driver/src/service/mod.rs` contains `DeviceListChangeType::Add`
    - `crates/monsgeek-driver/src/service/mod.rs` contains `DeviceListChangeType::Remove`
    - `crates/monsgeek-driver/src/service/mod.rs` does not emit `DeviceListChangeType::Change` in normal stream path
    - `cargo test -p monsgeek-driver watch_dev_list -- --nocapture` exits 0
  </acceptance_criteria>
  <done>
    - Stream contract now matches Init-then-delta behavior
    - Runtime add/remove transitions are emitted consistently
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement sendRawFeature/readRawFeature and sendMsg/readMsg as first-class split RPCs</name>
  <files>
    crates/monsgeek-driver/src/bridge_transport.rs
    crates/monsgeek-driver/src/service/mod.rs
    crates/monsgeek-driver/src/service/db_store.rs
    crates/monsgeek-transport/src/lib.rs
  </files>
  <read_first>
    crates/monsgeek-driver/src/bridge_transport.rs
    crates/monsgeek-driver/src/service/mod.rs
    crates/monsgeek-driver/src/service/db_store.rs
    crates/monsgeek-transport/src/lib.rs
    references/monsgeek-akko-linux/iot_driver_linux/src/grpc.rs
    references/monsgeek-akko-linux/iot_driver_linux/proto/driver.proto
    .planning/phases/03-grpc-web-bridge/03-CONTEXT.md
  </read_first>
  <action>
    Introduce a bridge transport adapter that supports split semantics:
    - send path: transmit command/frame immediately
    - read path: fetch the next pending response later
    Implement `send_raw_feature`/`read_raw_feature` using no-checksum semantics.
    Implement `send_msg`/`read_msg` using checksum selection from request.
    Implement `get_version` returning valid proto fields `base_version` and `time_stamp`.
    Implement in-memory DB operations required for MVP: `insert_db`, `get_item_from_db`.
    Keep store process-local and non-persistent for Phase 3.
    If current `monsgeek-transport` API is too narrow, extend it in-place rather than bypassing transport ownership/throttling rules.
    Decision traceability: implements D-10, D-11, D-12, D-13, D-14, D-15.
  </action>
  <verify>
    <automated>cd /home/whitebehemoth/Dev/Projects/monsgeek-firmware-driver && cargo test -p monsgeek-driver send_raw_feature read_raw_feature send_msg read_msg get_version db_insert_get -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `crates/monsgeek-driver/src/service/mod.rs` contains `async fn send_raw_feature`
    - `crates/monsgeek-driver/src/service/mod.rs` contains `async fn read_raw_feature`
    - `crates/monsgeek-driver/src/service/mod.rs` contains `async fn send_msg`
    - `crates/monsgeek-driver/src/service/mod.rs` contains `async fn read_msg`
    - `crates/monsgeek-driver/src/service/mod.rs` contains `async fn get_version`
    - `crates/monsgeek-driver/src/service/mod.rs` contains `async fn insert_db`
    - `crates/monsgeek-driver/src/service/mod.rs` contains `async fn get_item_from_db`
    - `crates/monsgeek-driver/src/service/db_store.rs` contains an in-memory map keyed by `(db_path, key)`
    - `crates/monsgeek-driver/src/service/mod.rs` routes all four methods through a shared bridge transport adapter
    - `cargo test -p monsgeek-driver send_raw_feature read_raw_feature send_msg read_msg get_version db_insert_get -- --nocapture` exits 0
  </acceptance_criteria>
  <done>
    - Raw and msg RPC families both operate via split send/read paths
    - Shared adapter preserves transport safety invariants
    - Version and DB compatibility RPCs are operational for MVP
    - Persistence behavior remains explicitly session-scoped
  </done>
</task>

</tasks>

<verification>
- `cargo test -p monsgeek-driver -- --nocapture`
- targeted RPC tests for watch stream, send/read split, and DB/version
</verification>

<success_criteria>
- MVP Phase 3 RPCs run with real semantics, not placeholders
- Device stream behavior matches locked decision (`Init` + `Add/Remove`)
- Both raw and checksum-aware message families are first-class and split
- In-memory DB compatibility works for session-scoped web app storage
</success_criteria>

<output>
After completion, create `.planning/phases/03-grpc-web-bridge/03-02-SUMMARY.md`
</output>
