---
phase: 03-grpc-web-bridge
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/monsgeek-driver/Cargo.toml
  - crates/monsgeek-driver/build.rs
  - crates/monsgeek-driver/proto/driver.proto
  - crates/monsgeek-driver/src/main.rs
  - crates/monsgeek-driver/src/lib.rs
  - crates/monsgeek-driver/src/pb.rs
  - crates/monsgeek-driver/src/service/mod.rs
autonomous: true
requirements: [GRPC-01, GRPC-07, GRPC-08]

must_haves:
  truths:
    - "Bridge starts on 127.0.0.1:3814 with gRPC-Web, HTTP/1, and CORS enabled"
    - "Full DriverGrpc contract is registered from generated proto types (no partial service)"
    - "Out-of-scope RPCs remain present as compatibility-safe stubs instead of missing endpoints"
  artifacts:
    - path: "crates/monsgeek-driver/proto/driver.proto"
      provides: "Canonical bridge wire contract with original method and field quirks"
      contains: "service DriverGrpc"
    - path: "crates/monsgeek-driver/src/service/mod.rs"
      provides: "Full DriverGrpc skeleton and compatibility-safe stub policy"
      contains: "impl DriverGrpc for DriverService"
    - path: "crates/monsgeek-driver/src/main.rs"
      provides: "Browser-ready gRPC-Web server bootstrap on localhost"
      contains: "accept_http1(true)"
  key_links:
    - from: "crates/monsgeek-driver/src/main.rs"
      to: "references/monsgeek-akko-linux/iot_driver_linux/src/main.rs"
      via: "tonic_web + CORS + HTTP/1 runtime shape"
      pattern: "tonic_web::enable"
    - from: "crates/monsgeek-driver/proto/driver.proto"
      to: "references/monsgeek-akko-linux/iot_driver_linux/proto/driver.proto"
      via: "wire-level contract parity"
      pattern: "rpc watchVender"
---

<objective>
Create a browser-compatible gRPC-Web bridge skeleton on `127.0.0.1:3814` with full `DriverGrpc` registration and compatibility-safe stubs.

Purpose:
- establish the exact wire contract surface early
- avoid contract drift by generating server traits from proto
- prove server runtime shape (`tonic-web` + HTTP/1 + CORS) before device logic

Output: compilable bridge crate with generated proto bindings and full service registration.
</objective>

<context>
@.planning/phases/03-grpc-web-bridge/03-CONTEXT.md
@.planning/phases/03-grpc-web-bridge/03-RESEARCH.md
@.planning/REQUIREMENTS.md
@references/monsgeek-akko-linux/iot_driver_linux/proto/driver.proto
@references/monsgeek-akko-linux/iot_driver_linux/src/main.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add proto generation and runtime dependencies for full DriverGrpc service</name>
  <files>
    crates/monsgeek-driver/Cargo.toml
    crates/monsgeek-driver/build.rs
    crates/monsgeek-driver/proto/driver.proto
    crates/monsgeek-driver/src/pb.rs
    crates/monsgeek-driver/src/lib.rs
  </files>
  <read_first>
    crates/monsgeek-driver/Cargo.toml
    crates/monsgeek-driver/src/main.rs
    references/monsgeek-akko-linux/iot_driver_linux/proto/driver.proto
    references/monsgeek-akko-linux/iot_driver_linux/Cargo.toml
    .planning/phases/03-grpc-web-bridge/03-CONTEXT.md
  </read_first>
  <action>
    Add `tonic`, `tonic-web`, `prost`, `tokio`, `tokio-stream`, `tower-http`, and build dependency `tonic-build`.
    Create `build.rs` that compiles `proto/driver.proto` with server code generation enabled.
    Copy vendor-compatible proto into `crates/monsgeek-driver/proto/driver.proto` without renaming methods or fields.
    Add `src/pb.rs` exposing `pub mod driver` from generated code.
    Add `src/lib.rs` that re-exports proto module and service module.
    Decision traceability: implements D-01, D-02.
  </action>
  <verify>
    <automated>cd /home/whitebehemoth/Dev/Projects/monsgeek-firmware-driver && cargo check -p monsgeek-driver</automated>
  </verify>
  <acceptance_criteria>
    - `crates/monsgeek-driver/proto/driver.proto` contains `service DriverGrpc`
    - `crates/monsgeek-driver/proto/driver.proto` contains `rpc watchVender(Empty) returns (stream VenderMsg);`
    - `crates/monsgeek-driver/build.rs` contains `compile_protos`
    - `crates/monsgeek-driver/src/pb.rs` contains `pub mod driver`
    - `cargo check -p monsgeek-driver` exits 0
  </acceptance_criteria>
  <done>
    - Proto code generation works from local `driver.proto`
    - Generated service trait is available to implement full contract
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement full DriverGrpc skeleton with compatibility-safe stubs</name>
  <files>
    crates/monsgeek-driver/src/service/mod.rs
    crates/monsgeek-driver/src/lib.rs
  </files>
  <read_first>
    crates/monsgeek-driver/src/lib.rs
    crates/monsgeek-driver/src/pb.rs
    references/monsgeek-akko-linux/iot_driver_linux/src/grpc.rs
    .planning/phases/03-grpc-web-bridge/03-CONTEXT.md
  </read_first>
  <action>
    Create `DriverService` implementing generated `DriverGrpc` trait.
    Define streaming associated types required by proto.
    Implement all RPC methods from trait; for non-MVP methods return deterministic compatibility-safe responses (empty streams, empty payloads, or `ResSend { err: \"not supported yet\" }` depending on response type).
    Keep method names and message fields untouched at wire level.
    Decision traceability: implements D-01, D-02, D-12.
  </action>
  <verify>
    <automated>cd /home/whitebehemoth/Dev/Projects/monsgeek-firmware-driver && cargo test -p monsgeek-driver -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `crates/monsgeek-driver/src/service/mod.rs` contains `impl DriverGrpc for DriverService`
    - `crates/monsgeek-driver/src/service/mod.rs` contains handlers for `watch_dev_list`, `send_raw_feature`, `read_raw_feature`, `send_msg`, `read_msg`, `insert_db`, `get_item_from_db`, `get_version`
    - `crates/monsgeek-driver/src/service/mod.rs` contains handler for `watch_vender`
    - `cargo test -p monsgeek-driver -- --nocapture` exits 0
  </acceptance_criteria>
  <done>
    - Full DriverGrpc trait compiles with all methods present
    - Unsupported RPC policy is explicit and compatibility-safe
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire server bootstrap for browser gRPC-Web on localhost:3814</name>
  <files>
    crates/monsgeek-driver/src/main.rs
  </files>
  <read_first>
    crates/monsgeek-driver/src/main.rs
    references/monsgeek-akko-linux/iot_driver_linux/src/main.rs
    .planning/REQUIREMENTS.md
    .planning/phases/03-grpc-web-bridge/03-RESEARCH.md
  </read_first>
  <action>
    Replace placeholder main with async Tokio server startup.
    Bind to `127.0.0.1:3814`.
    Wrap `DriverGrpcServer::new(DriverService::new())` with `tonic_web::enable`.
    Apply permissive CORS via `tower_http::cors::CorsLayer` (`Any` origin/headers/methods/expose_headers).
    Force HTTP/1 support with `accept_http1(true)`.
    Decision traceability: implements D-01, D-02.
  </action>
  <verify>
    <automated>cd /home/whitebehemoth/Dev/Projects/monsgeek-firmware-driver && cargo run -p monsgeek-driver -- --help >/dev/null 2>&1 || true && cargo check -p monsgeek-driver</automated>
  </verify>
  <acceptance_criteria>
    - `crates/monsgeek-driver/src/main.rs` contains `127.0.0.1:3814`
    - `crates/monsgeek-driver/src/main.rs` contains `tonic_web::enable`
    - `crates/monsgeek-driver/src/main.rs` contains `.accept_http1(true)`
    - `crates/monsgeek-driver/src/main.rs` contains `CorsLayer::new()`
    - `cargo check -p monsgeek-driver` exits 0
  </acceptance_criteria>
  <done>
    - Bridge boot path is browser-compatible for gRPC-Web clients
    - Server binds to expected localhost endpoint and compiles
  </done>
</task>

</tasks>

<verification>
- `cargo check -p monsgeek-driver`
- `cargo test -p monsgeek-driver -- --nocapture`
</verification>

<success_criteria>
- Bridge process starts with gRPC-Web-compatible server runtime shape on localhost:3814
- Full `DriverGrpc` service is registered from generated proto
- Non-MVP RPCs are present and compatibility-safe instead of missing
</success_criteria>

<output>
After completion, create `.planning/phases/03-grpc-web-bridge/03-01-SUMMARY.md`
</output>
