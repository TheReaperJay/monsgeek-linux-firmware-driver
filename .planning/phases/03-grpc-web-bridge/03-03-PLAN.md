---
phase: 03-grpc-web-bridge
plan: 03
type: execute
wave: 3
depends_on: [03-02]
files_modified:
  - crates/monsgeek-driver/tests/grpc_contract_tests.rs
  - crates/monsgeek-driver/tests/grpc_watch_stream_tests.rs
  - crates/monsgeek-driver/tests/grpc_db_tests.rs
  - crates/monsgeek-driver/tests/mock_transport.rs
  - .planning/STATE.md
  - .planning/phases/03-grpc-web-bridge/03-VALIDATION.md
autonomous: false
requirements: [GRPC-01, GRPC-02, GRPC-03, GRPC-04, GRPC-05, GRPC-06, GRPC-07, GRPC-08]

must_haves:
  truths:
    - "Phase 3 is closed only with both automated contract coverage and human browser verification"
    - "Validation and project state files reflect real evidence, not assumed completion"
    - "Blocking checkpoint includes an automated pre-check before manual sign-off"
  artifacts:
    - path: "crates/monsgeek-driver/tests/grpc_contract_tests.rs"
      provides: "Contract/runtime assertions for service shape and compatibility behavior"
      contains: "grpc_full_service_contract_present"
    - path: ".planning/phases/03-grpc-web-bridge/03-VALIDATION.md"
      provides: "Per-requirement verification map and Nyquist status for Phase 3"
      contains: "nyquist_compliant"
    - path: ".planning/STATE.md"
      provides: "Authoritative milestone progression and next-phase readiness"
      contains: "Ready for Phase 04"
  key_links:
    - from: "crates/monsgeek-driver/tests/grpc_watch_stream_tests.rs"
      to: "crates/monsgeek-driver/src/service/mod.rs"
      via: "verify Init/Add/Remove stream semantics"
      pattern: "DeviceListChangeType::Init"
    - from: ".planning/phases/03-grpc-web-bridge/03-VALIDATION.md"
      to: ".planning/REQUIREMENTS.md"
      via: "trace GRPC-01..08 to executed validation evidence"
      pattern: "GRPC-0"
---

<objective>
Harden and verify the Phase 3 bridge against the actual compatibility contract, then close the phase with explicit evidence.

Purpose:
- prevent false positives from compile-only checks
- validate full contract surface and MVP runtime behavior
- capture manual browser verification as a blocking checkpoint

Output: targeted test coverage for Phase 3 requirements and state update proving readiness for Phase 4.
</objective>

<context>
@.planning/phases/03-grpc-web-bridge/03-CONTEXT.md
@.planning/phases/03-grpc-web-bridge/03-RESEARCH.md
@.planning/phases/03-grpc-web-bridge/03-VALIDATION.md
@references/monsgeek-akko-linux/iot_driver_linux/proto/driver.proto
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add contract and runtime integration tests for GRPC-01..08</name>
  <files>
    crates/monsgeek-driver/tests/grpc_contract_tests.rs
    crates/monsgeek-driver/tests/grpc_watch_stream_tests.rs
    crates/monsgeek-driver/tests/grpc_db_tests.rs
    crates/monsgeek-driver/tests/mock_transport.rs
  </files>
  <read_first>
    crates/monsgeek-driver/src/main.rs
    crates/monsgeek-driver/src/service/mod.rs
    crates/monsgeek-driver/src/bridge_transport.rs
    references/monsgeek-akko-linux/iot_driver_linux/proto/driver.proto
    .planning/phases/03-grpc-web-bridge/03-VALIDATION.md
  </read_first>
  <action>
    Create tests that prove:
    - full service contract is registered and callable
    - server runtime has gRPC-Web prerequisites (HTTP/1 + CORS behavior)
    - `sendRawFeature/readRawFeature` and `sendMsg/readMsg` execute split behavior
    - `watchDevList` emits `Init` first and then add/remove deltas
    - `getVersion` returns valid proto fields
    - `insertDb/getItemFromDb` round-trip works in-memory
    Use mock transport fixtures for determinism instead of requiring hardware in unit/integration tests.
    Decision traceability: implements D-01, D-02, D-07, D-08, D-10, D-12, D-13.
  </action>
  <verify>
    <automated>cd /home/whitebehemoth/Dev/Projects/monsgeek-firmware-driver && cargo test -p monsgeek-driver -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `crates/monsgeek-driver/tests/grpc_contract_tests.rs` exists and contains tests named for full service contract and server startup
    - `crates/monsgeek-driver/tests/grpc_watch_stream_tests.rs` exists and contains an `Init`-first assertion
    - `crates/monsgeek-driver/tests/grpc_db_tests.rs` exists and contains insert/get roundtrip assertion
    - `crates/monsgeek-driver/tests/mock_transport.rs` exists and is used by at least one RPC test module
    - `cargo test -p monsgeek-driver -- --nocapture` exits 0
  </acceptance_criteria>
  <done>
    - Automated tests exist and execute for each Phase 3 requirement family
    - Contract and runtime behavior are validated without relying on ad-hoc manual checks
  </done>
</task>

<task type="auto">
  <name>Task 2: Update validation/state artifacts to reflect actual coverage and phase outcome</name>
  <files>
    .planning/phases/03-grpc-web-bridge/03-VALIDATION.md
    .planning/STATE.md
  </files>
  <read_first>
    .planning/phases/03-grpc-web-bridge/03-VALIDATION.md
    .planning/STATE.md
    .planning/ROADMAP.md
    .planning/REQUIREMENTS.md
  </read_first>
  <action>
    Mark completed validation rows in `03-VALIDATION.md`, set `wave_0_complete` and `nyquist_compliant` truthfully based on implemented tests, and align `STATE.md` with Phase 3 completion readiness.
    Keep unresolved items explicit; do not mark complete without evidence.
    Decision traceability: implements D-16 and enforces all locked-scope boundaries in project state.
  </action>
  <verify>
    <automated>cd /home/whitebehemoth/Dev/Projects/monsgeek-firmware-driver && rg -n "phase 03|Ready for Phase 04|nyquist_compliant|wave_0_complete" .planning/phases/03-grpc-web-bridge/03-VALIDATION.md .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/03-grpc-web-bridge/03-VALIDATION.md` contains updated truth values for `nyquist_compliant` and `wave_0_complete`
    - `.planning/STATE.md` reflects post-Phase-3 status and next-phase focus
    - no requirement in GRPC-01..08 is left unmentioned in validation/state closeout notes
  </acceptance_criteria>
  <done>
    - Validation and state documents accurately reflect observed outcomes
    - Any unresolved gaps are explicitly carried forward
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Browser-level compatibility checkpoint with real configurator</name>
  <files>
    crates/monsgeek-driver/src/main.rs
    crates/monsgeek-driver/src/service/mod.rs
  </files>
  <action>
    Perform real-host manual verification:
    - start bridge server on localhost:3814
    - open `https://app.monsgeek.com`
    - confirm device appears in picker
    - confirm at least one command roundtrip succeeds through bridge path
    - confirm plug/unplug updates reflect in UI
    Record results and blockers before phase summary.
    Decision traceability: enforces D-02, D-07, D-08 in real browser runtime.
  </action>
  <verify>
    <automated>cd /home/whitebehemoth/Dev/Projects/monsgeek-firmware-driver && cargo test -p monsgeek-driver grpc_server_starts_http1 grpc_watch_dev_list_init_add_remove -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - user confirms browser can connect without CORS/preflight failures
    - user confirms device appears and can execute at least one command/read path
    - user confirms device list updates on unplug/replug
    - unresolved behavior (if any) is documented before phase summary
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `cargo test -p monsgeek-driver -- --nocapture`
- browser/manual checkpoint at `https://app.monsgeek.com`
</verification>

<success_criteria>
- GRPC-01..08 are backed by automated tests plus manual browser verification where required
- Contract and stream semantics are validated, not assumed
- Phase 3 closeout evidence is captured in validation and project state docs
</success_criteria>

<output>
After completion, create `.planning/phases/03-grpc-web-bridge/03-03-SUMMARY.md`
</output>
