---
phase: 08-firmware-update
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - crates/monsgeek-driver/Cargo.toml
  - crates/monsgeek-driver/src/main.rs
  - crates/monsgeek-driver/src/service/mod.rs
  - crates/monsgeek-driver/src/service/firmware_update.rs
  - crates/monsgeek-driver/tests/ota_stream.rs
  - crates/monsgeek-cli/src/commands.rs
  - scripts/firmware-flash-checkpoint.sh
  - docs/firmware-update.md
autonomous: false
requirements:
  - FW-01
  - FW-02
  - FW-03
  - FW-04

must_haves:
  truths:
    - "`upgradeOTAGATT` is implemented and streams phase + numeric progress"
    - "Bridge OTA path is disabled by default and requires explicit runtime enablement"
    - "Bootloader entry has bounded retry policy: one controlled retry, then hard fail"
    - "Transfer completion validates checksum/integrity and fails hard on mismatch/incomplete transfer"
    - "Success is reported only after post-flash normal-mode re-enumeration and version verification"
    - "Bridge and CLI use the same shared firmware engine implementation"
    - "Failure responses include explicit recovery guidance"
  artifacts:
    - path: "crates/monsgeek-driver/src/service/mod.rs"
      provides: "upgrade_otagatt RPC wiring and runtime gate enforcement"
      contains: "async fn upgrade_otagatt"
    - path: "crates/monsgeek-driver/src/service/firmware_update.rs"
      provides: "Bridge adapter over shared firmware engine with progress mapping"
      contains: "stream_progress"
    - path: "crates/monsgeek-driver/tests/ota_stream.rs"
      provides: "Progress streaming, gate, retry, and failure-mode tests"
      contains: "upgrade_otagatt_requires_enable_flag"
    - path: "scripts/firmware-flash-checkpoint.sh"
      provides: "Manual guarded verification runbook command sequence"
      contains: "FLASH M5W"
  key_links:
    - from: "crates/monsgeek-driver/src/service/firmware_update.rs"
      to: "crates/monsgeek-firmware/src/engine.rs"
      via: "shared engine drives both CLI and bridge OTA flow"
      pattern: "FirmwareEngine"
    - from: "crates/monsgeek-driver/src/service/mod.rs"
      to: "crates/monsgeek-driver/proto/driver.proto"
      via: "upgradeOTAGATT RPC stream emits Progress { progress, err }"
      pattern: "Progress"
---

<objective>
Integrate firmware update into the bridge with safe-by-default gating, progress streaming, strict post-flash verification, and operational runbook coverage.

Purpose: Deliver FW-02 end-to-end while preserving FW-01/FW-03/FW-04 guarantees through shared engine reuse and explicit runtime safeguards.
Output: Working `upgradeOTAGATT` stream implementation, runtime enable flag, regression tests, and manual hardware checkpoint procedure.
</objective>

<execution_context>
@.codex/get-shit-done/workflows/execute-plan.md
@.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-firmware-update/08-CONTEXT.md
@.planning/phases/08-firmware-update/08-RESEARCH.md
@.planning/phases/08-firmware-update/08-VALIDATION.md
@.planning/phases/08-firmware-update/08-01-SUMMARY.md

<interfaces>
From crates/monsgeek-driver/proto/driver.proto:
```proto
rpc upgradeOTAGATT(OTAUpgrade) returns (stream Progress);
message Progress { float progress = 1; string err = 2; }
```

From crates/monsgeek-driver/src/service/mod.rs:
```rust
async fn upgrade_otagatt(...) -> Result<Response<Self::upgradeOTAGATTStream>, Status> {
    Ok(Response::new(Box::pin(stream::empty())))
}
```

From references/monsgeek-akko-linux/iot_driver_linux/src/flash.rs:
- phases: EnteringBootloader, WaitingForBootloader, StartingTransfer, TransferringData, CompletingTransfer, WaitingForReboot
- one bootloader wait timeout path
- single-path transfer and explicit completion handling

From CONTEXT decisions:
- bridge OTA must be runtime-gated and disabled by default
- success requires normal-mode re-enumeration and version verification
- integrity mismatch/incomplete transfer must be hard failure with recovery instructions
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire driver runtime OTA gate and bridge adapter over shared engine</name>
  <files>crates/monsgeek-driver/Cargo.toml, crates/monsgeek-driver/src/main.rs, crates/monsgeek-driver/src/service/mod.rs, crates/monsgeek-driver/src/service/firmware_update.rs</files>
  <read_first>
    - crates/monsgeek-driver/src/main.rs
    - crates/monsgeek-driver/src/service/mod.rs
    - crates/monsgeek-driver/proto/driver.proto
    - crates/monsgeek-firmware/src/engine.rs
    - .planning/phases/08-firmware-update/08-CONTEXT.md
  </read_first>
  <action>
Add `monsgeek-firmware` dependency to driver crate.

Introduce explicit runtime gate:
- Add CLI/startup flag in `crates/monsgeek-driver/src/main.rs`: `--enable-ota`.
- Thread this setting into `DriverService` construction (for example `DriverService::new_with_flags(DriverFlags { ota_enabled: bool })`).
- Keep default behavior `ota_enabled = false`.

Implement `crates/monsgeek-driver/src/service/firmware_update.rs`:
- Bridge adapter that accepts `OTAUpgrade { devPath, file_buf }`.
- Calls shared engine preflight + execution.
- Maps engine progress events into proto `Progress { progress, err }`.
- Emits phase-prefixed progress strings in `err` while operation is in progress (for example: `phase=transfer_chunks sent=37/128`), and leaves `err` empty on completed terminal success.

Update `upgrade_otagatt` in `crates/monsgeek-driver/src/service/mod.rs`:
- If gate disabled: return `Status::failed_precondition("OTA disabled; start monsgeek-driver with --enable-ota")`.
- If enabled: spawn stream execution and return stream.
- Ensure stream terminates with explicit error item and then closes on failure.
  </action>
  <verify>
    <automated>cd /home/whitebehemoth/Dev/Projects/monsgeek-firmware-driver && cargo check -p monsgeek-driver</automated>
  </verify>
  <acceptance_criteria>
    - `crates/monsgeek-driver/src/main.rs` contains `--enable-ota`
    - `crates/monsgeek-driver/src/service/mod.rs` no longer returns `stream::empty()` from `upgrade_otagatt`
    - `crates/monsgeek-driver/src/service/mod.rs` contains disabled-gate message for OTA
    - `crates/monsgeek-driver/src/service/firmware_update.rs` exists and references `monsgeek_firmware`
    - `cargo check -p monsgeek-driver` exits 0
  </acceptance_criteria>
  <done>Driver has explicit OTA runtime gate and a real bridge adapter that streams firmware engine progress</done>
</task>

<task type="auto">
  <name>Task 2: Implement retry/integrity/post-verify behavior and regression tests</name>
  <files>crates/monsgeek-driver/src/service/firmware_update.rs, crates/monsgeek-driver/tests/ota_stream.rs, crates/monsgeek-cli/src/commands.rs</files>
  <read_first>
    - crates/monsgeek-driver/src/service/firmware_update.rs
    - crates/monsgeek-firmware/src/preflight.rs
    - crates/monsgeek-firmware/src/engine.rs
    - references/monsgeek-akko-linux/iot_driver_linux/src/flash.rs
    - crates/monsgeek-transport/src/usb.rs
  </read_first>
  <action>
Implement concrete behavior in bridge adapter:
- Bootloader entry retry policy: if first transition wait times out, perform exactly one retry; second timeout returns hard failure.
- Integrity mismatch/incomplete transfer from engine maps to terminal failure with recovery message including at least:
  - "device may still be in bootloader mode"
  - "re-run with a known-good image"
  - "use physical recovery path if device no longer enumerates"
- Post-flash verification must execute:
  1. wait for normal-mode re-enumeration
  2. query `GET_USB_VERSION`
  3. query `GET_REV` when available
  4. only then emit final success completion.

Add `crates/monsgeek-driver/tests/ota_stream.rs` covering:
- `upgrade_otagatt_requires_enable_flag`
- `upgrade_otagatt_streams_phase_progress_in_order`
- `upgrade_otagatt_retries_bootloader_once_then_fails`
- `upgrade_otagatt_reports_recovery_guidance_on_integrity_failure`
- `upgrade_otagatt_success_requires_post_verify_queries`

Update CLI firmware command output handling (if needed) so bridge/CLI phase names and failure wording remain aligned (same recovery terms and phase labels).
  </action>
  <verify>
    <automated>cd /home/whitebehemoth/Dev/Projects/monsgeek-firmware-driver && cargo test -p monsgeek-driver ota_ -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `ota_stream.rs` contains `upgrade_otagatt_requires_enable_flag`
    - `ota_stream.rs` contains `upgrade_otagatt_retries_bootloader_once_then_fails`
    - failure path text includes `device may still be in bootloader mode`
    - success path test asserts post-verify query calls (`GET_USB_VERSION`, and `GET_REV` conditional)
    - `cargo test -p monsgeek-driver ota_` exits 0
  </acceptance_criteria>
  <done>Bridge OTA path has deterministic retry/failure semantics and regression tests for safety-critical behaviors</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Guarded hardware firmware update checkpoint on M5W</name>
  <files>scripts/firmware-flash-checkpoint.sh, docs/firmware-update.md</files>
  <action>
Create `scripts/firmware-flash-checkpoint.sh` documenting and automating the guarded manual path:
1. Start driver with OTA enabled.
2. Run CLI preflight validation on test image.
3. Require typed phrase `FLASH M5W`.
4. Execute flash and capture streamed phase output.
5. Verify post-flash `firmware version` query.
6. Save session metadata + backup path into an output log.

Add `docs/firmware-update.md` with:
- prerequisites
- dual-flag non-interactive policy
- expected progress phases
- known failure modes and recovery instructions (including DFU fallback notes)
- explicit warning that update erases app config region.

Human checkpoint procedure:
- Use known-good M5W firmware image.
- Confirm phase stream order and successful post-verify.
- Confirm failed image path returns hard error and recovery guidance.
- Confirm OTA RPC is blocked when driver started without `--enable-ota`.
  </action>
  <verify>
    <human>Run guarded hardware checkpoint and capture command output snippets for success and intentional-failure scenarios.</human>
  </verify>
  <acceptance_criteria>
    - `scripts/firmware-flash-checkpoint.sh` exists and contains `FLASH M5W`
    - `docs/firmware-update.md` contains `--enable-ota` and recovery section
    - Human verification confirms success flow, blocked flow (gate disabled), and integrity-failure flow
  </acceptance_criteria>
  <done>Real hardware checkpoint validates bridge OTA safety and operational runbook end-to-end</done>
  <resume-signal>Type "approved" if all checks pass, or report observed failure output</resume-signal>
</task>

</tasks>

<verification>
- `cargo check -p monsgeek-driver`
- `cargo test -p monsgeek-driver ota_ -- --nocapture`
- `cargo test -p monsgeek-cli firmware_ -- --nocapture`
- `bash -n scripts/firmware-flash-checkpoint.sh`
</verification>

<success_criteria>
- `upgradeOTAGATT` is implemented and no longer a stub
- OTA is disabled by default and only enabled by explicit runtime flag
- FW-02 transfer flow includes bounded retry and hard integrity failure behavior
- Final success is contingent on post-flash version verification
- Operational docs/script cover safe execution and recovery guidance
</success_criteria>

<output>
After completion, create `.planning/phases/08-firmware-update/08-02-SUMMARY.md`
</output>
