---
phase: 06-macros-device-specific-advanced-features
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/monsgeek-driver/src/service/mod.rs
autonomous: true
requirements:
  - MACR-02
  - MAG-01
  - MAG-02
  - MAG-03
  - MAG-04

must_haves:
  truths:
    - "SET_MACRO with macro_index > 49 is rejected with invalid_argument"
    - "SET_MACRO with chunk_page > 9 is rejected with invalid_argument"
    - "SET_FN with key_index out of device bounds is rejected with bounds violation"
    - "SET_FN with profile > 3 is rejected with invalid_argument"
    - "Magnetic SET commands on a non-magnetic device are rejected with failed_precondition"
    - "Magnetic SET commands on a magnetic-capable device pass through without error"
    - "Non-dangerous commands continue to pass through without validation"
  artifacts:
    - path: "crates/monsgeek-driver/src/service/mod.rs"
      provides: "SET_MACRO, SET_FN, and magnetic command gating branches in validate_dangerous_write"
      contains: "SET_MACRO macro_index"
  key_links:
    - from: "crates/monsgeek-driver/src/service/mod.rs"
      to: "monsgeek_transport::bounds::validate_write_request"
      via: "SET_FN reuses existing bounds validation"
      pattern: "validate_write_request.*definition.*key_index"
    - from: "crates/monsgeek-driver/src/service/mod.rs"
      to: "monsgeek_protocol::DeviceDefinition::has_magnetism"
      via: "magnetic command gating"
      pattern: "has_magnetism"
---

<objective>
Extend validate_dangerous_write with SET_MACRO bounds, SET_FN bounds, and magnetic command gating, with comprehensive unit tests for all new branches.

Purpose: Prevents firmware flash corruption from OOB macro writes (macro_index > 49 corrupts userpic/calibration flash), prevents OOB key index writes via SET_FN, and blocks magnetic switch commands from reaching non-magnetic devices where they produce undefined behavior. These are the deferred Phase 4 bounds gaps.

Output: Extended validate_dangerous_write function with unit tests proving all boundary conditions.
</objective>

<execution_context>
@/home/whitebehemoth/Dev/Projects/monsgeek-firmware-driver/.claude/get-shit-done/workflows/execute-plan.md
@/home/whitebehemoth/Dev/Projects/monsgeek-firmware-driver/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-macros-device-specific-advanced-features/06-CONTEXT.md
@.planning/phases/06-macros-device-specific-advanced-features/06-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From crates/monsgeek-driver/src/service/mod.rs (line 45):
```rust
pub(crate) fn validate_dangerous_write(
    definition: &DeviceDefinition,
    msg: &[u8],
) -> Result<(), Status> {
    // Currently handles: set_keymatrix, set_keymatrix_simple
    // Must add: SET_MACRO, SET_FN, magnetic command gating
}
```

From crates/monsgeek-protocol/src/cmd.rs:
```rust
pub const SET_MACRO: u8 = 0x0B;
pub const SET_FN: u8 = 0x10;
pub const SET_MAGNETISM_REPORT: u8 = 0x1B;
pub const SET_MAGNETISM_CAL: u8 = 0x1C;
pub const SET_KEY_MAGNETISM_MODE: u8 = 0x1D;
pub const SET_MAGNETISM_MAX_CAL: u8 = 0x1E;
pub const SET_MULTI_MAGNETISM: u8 = 0x65;
```

From crates/monsgeek-protocol/src/protocol.rs:
```rust
pub struct CommandTable {
    pub set_macro: u8,  // 0x0B shared, 0x08 YiChip override
    // SET_FN is NOT in CommandTable -- it is shared constant cmd::SET_FN = 0x10
}
```

From crates/monsgeek-protocol/src/device.rs:
```rust
impl DeviceDefinition {
    pub fn has_magnetism(&self) -> bool { ... }
    pub fn commands(&self) -> CommandTable { ... }
}
// Fields: key_count: Option<u8>, layer: Option<u8>, display_name: String
```

From crates/monsgeek-transport/src/bounds.rs:
```rust
pub fn validate_write_request(
    device: &DeviceDefinition,
    key_index: u16,
    layer: u8,
) -> Result<(), TransportError>;
```

From crates/monsgeek-driver/src/service/mod.rs (line 34):
```rust
const MAX_PROFILE: u8 = 3;
```

Existing test helper (line 1073):
```rust
fn make_test_definition() -> monsgeek_protocol::DeviceDefinition {
    // id: 9999, key_count: Some(108), layer: Some(4)
    // magnetism: None, no_magnetic_switch: None (defaults to !has_magnetism)
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add SET_MACRO, SET_FN, and magnetic command gating to validate_dangerous_write</name>
  <files>crates/monsgeek-driver/src/service/mod.rs</files>
  <read_first>
    - crates/monsgeek-driver/src/service/mod.rs
    - crates/monsgeek-protocol/src/cmd.rs
    - crates/monsgeek-protocol/src/device.rs
    - crates/monsgeek-transport/src/bounds.rs
    - .planning/phases/06-macros-device-specific-advanced-features/06-RESEARCH.md
  </read_first>
  <action>
Add three new validation branches to `validate_dangerous_write` in `crates/monsgeek-driver/src/service/mod.rs`, inserted after the existing `set_keymatrix_simple` block and before `Ok(())`:

**Branch 1: SET_MACRO bounds**
Compare `cmd` against `commands.set_macro` (resolves per-device via CommandTable — 0x0B for M5W, 0x08 for some YiChip devices). Add constants `const MAX_MACRO_INDEX: u8 = 49;` and `const MAX_CHUNK_PAGE: u8 = 9;` at module level near `MAX_PROFILE`. Require `msg.len() >= 3`. Extract `macro_index = msg[1]`, `chunk_page = msg[2]`. Reject `macro_index > MAX_MACRO_INDEX` with `Status::invalid_argument(format!("SET_MACRO macro_index {} exceeds max {}", macro_index, MAX_MACRO_INDEX))`. Reject `chunk_page > MAX_CHUNK_PAGE` with `Status::invalid_argument(format!("SET_MACRO chunk_page {} exceeds max {}", chunk_page, MAX_CHUNK_PAGE))`.

**Branch 2: SET_FN bounds**
Compare `cmd` against `cmd::SET_FN` (shared constant 0x10, not in CommandTable). Require `msg.len() >= 4`. Extract `profile = msg[2]`, `key_index = msg[3] as u16`. Wire format: `[cmd=0x10, fn_sys, profile, key_index, ...]`. Reject `profile > MAX_PROFILE` with the same format as SET_KEYMATRIX. Call `monsgeek_transport::bounds::validate_write_request(definition, key_index, 0).map_err(|e| Status::invalid_argument(e.to_string()))?` — layer=0 because SET_FN does not carry a layer byte.

**Branch 3: Magnetic command gating**
Define a local array: `let magnetic_write_cmds: [u8; 5] = [cmd::SET_MAGNETISM_REPORT, cmd::SET_MAGNETISM_CAL, cmd::SET_KEY_MAGNETISM_MODE, cmd::SET_MAGNETISM_MAX_CAL, cmd::SET_MULTI_MAGNETISM];`. If `magnetic_write_cmds.contains(&cmd) && !definition.has_magnetism()`, return `Err(Status::failed_precondition(format!("{} rejected: device {} does not support magnetic switches", cmd::name(cmd), definition.display_name)))`.

Add `use monsgeek_protocol::cmd;` at the top of the function body or at the module level if not already imported (verify — the current file uses `monsgeek_protocol` types but may not import `cmd` directly).

Update the function doc comment to mention SET_MACRO, SET_FN, and magnetic command gating alongside the existing SET_KEYMATRIX mentions.
  </action>
  <verify>
    <automated>cd /home/whitebehemoth/Dev/Projects/monsgeek-firmware-driver && cargo test -p monsgeek-driver 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - validate_dangerous_write in service/mod.rs contains `commands.set_macro` comparison
    - validate_dangerous_write contains `cmd::SET_FN` comparison
    - validate_dangerous_write contains `has_magnetism()` check
    - Module contains `const MAX_MACRO_INDEX: u8 = 49`
    - Module contains `const MAX_CHUNK_PAGE: u8 = 9`
    - SET_MACRO branch extracts msg[1] as macro_index and msg[2] as chunk_page
    - SET_FN branch extracts msg[2] as profile and msg[3] as key_index
    - Magnetic gating uses `Status::failed_precondition` (not `invalid_argument`)
    - `cargo test -p monsgeek-driver` passes (existing tests still green)
  </acceptance_criteria>
  <done>validate_dangerous_write has SET_MACRO, SET_FN, and magnetic gating branches, all existing tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Unit tests for SET_MACRO, SET_FN, and magnetic command validation</name>
  <files>crates/monsgeek-driver/src/service/mod.rs</files>
  <read_first>
    - crates/monsgeek-driver/src/service/mod.rs
    - crates/monsgeek-protocol/src/cmd.rs
  </read_first>
  <behavior>
    - test_set_macro_valid_passes: msg with cmd=set_macro, macro_index=0, chunk_page=0 passes Ok
    - test_set_macro_boundary_valid: msg with macro_index=49, chunk_page=9 passes Ok
    - test_set_macro_index_oob_rejected: macro_index=50 returns err containing "macro_index"
    - test_set_macro_chunk_page_oob_rejected: chunk_page=10 returns err containing "chunk_page"
    - test_set_macro_short_buffer_rejected: msg of 2 bytes returns err containing "too short"
    - test_set_fn_valid_passes: msg with cmd=SET_FN, profile=0, key_index=50 passes Ok
    - test_set_fn_boundary_valid: profile=3, key_index=107 passes Ok
    - test_set_fn_profile_oob_rejected: profile=4 returns err containing "profile"
    - test_set_fn_key_index_oob_rejected: key_index=108 returns err containing "bounds violation"
    - test_set_fn_short_buffer_rejected: msg of 3 bytes returns err containing "too short"
    - test_magnetic_cmd_non_magnetic_device_rejected: SET_MAGNETISM_CAL on device with no_magnetic_switch=true returns err containing "magnetic switches"
    - test_magnetic_cmd_magnetic_device_passes: SET_MAGNETISM_CAL on device with magnetism=true passes Ok
    - test_all_magnetic_cmds_gated: each of the 5 magnetic SET commands rejected on non-magnetic device
    - test_magnetic_read_cmds_not_gated: GET_MULTI_MAGNETISM and GET_KEY_MAGNETISM_MODE pass through (they are not write commands)
  </behavior>
  <action>
Add tests to the existing `#[cfg(test)] mod tests` block in `crates/monsgeek-driver/src/service/mod.rs`.

Add two new test helper functions:

1. `make_test_definition_magnetic()` — same as `make_test_definition()` but with `magnetism: Some(true)` instead of `None`.
2. `make_test_definition_non_magnetic()` — same as `make_test_definition()` but with `no_magnetic_switch: Some(true)`.
3. `make_set_macro_msg(cmd: u8, macro_index: u8, chunk_page: u8)` — returns 64-byte vec with msg[0]=cmd, msg[1]=macro_index, msg[2]=chunk_page.
4. `make_set_fn_msg(profile: u8, key_index: u8)` — returns 64-byte vec with msg[0]=cmd::SET_FN (0x10), msg[1]=0 (fn_sys), msg[2]=profile, msg[3]=key_index.

For SET_MACRO tests: use `make_test_definition().commands().set_macro` to get the device-specific command byte (will be 0x0B since the default test definition has no command overrides and defaults to shared).

For magnetic tests: use `make_test_definition_non_magnetic()` for rejection tests, `make_test_definition_magnetic()` for passthrough tests. Test all 5 magnetic SET constants: `cmd::SET_MAGNETISM_REPORT`, `cmd::SET_MAGNETISM_CAL`, `cmd::SET_KEY_MAGNETISM_MODE`, `cmd::SET_MAGNETISM_MAX_CAL`, `cmd::SET_MULTI_MAGNETISM`.

For the "read commands not gated" test: verify that `cmd::GET_MULTI_MAGNETISM` and `cmd::GET_KEY_MAGNETISM_MODE` pass through even on a non-magnetic device (they are GET commands, not gated).
  </action>
  <verify>
    <automated>cd /home/whitebehemoth/Dev/Projects/monsgeek-firmware-driver && cargo test -p monsgeek-driver -- validate_dangerous_write 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - mod.rs test module contains `fn test_set_macro_valid_passes`
    - mod.rs test module contains `fn test_set_macro_index_oob_rejected`
    - mod.rs test module contains `fn test_set_macro_chunk_page_oob_rejected`
    - mod.rs test module contains `fn test_set_fn_valid_passes`
    - mod.rs test module contains `fn test_set_fn_key_index_oob_rejected`
    - mod.rs test module contains `fn test_magnetic_cmd_non_magnetic_device_rejected`
    - mod.rs test module contains `fn test_magnetic_cmd_magnetic_device_passes`
    - `cargo test -p monsgeek-driver -- validate_dangerous_write` shows all tests passing
    - `cargo test -p monsgeek-driver` shows full suite passing (no regressions)
  </acceptance_criteria>
  <done>All SET_MACRO, SET_FN, and magnetic command validation tests pass, full driver test suite green</done>
</task>

</tasks>

<verification>
- `cargo test -p monsgeek-driver` — all existing + new tests pass
- `cargo test --workspace` — no regressions across workspace
- validate_dangerous_write rejects: SET_MACRO macro_index>49, SET_MACRO chunk_page>9, SET_FN profile>3, SET_FN key_index>=key_count, magnetic commands on non-magnetic devices
- validate_dangerous_write passes: valid SET_MACRO, valid SET_FN, magnetic commands on magnetic devices, all non-dangerous commands
</verification>

<success_criteria>
- Zero test failures in `cargo test -p monsgeek-driver`
- Zero test failures in `cargo test --workspace`
- All 14+ new unit tests covering boundary conditions for SET_MACRO, SET_FN, and magnetic gating
- validate_dangerous_write doc comment updated to reflect new scope
</success_criteria>

<output>
After completion, create `.planning/phases/06-macros-device-specific-advanced-features/06-01-SUMMARY.md`
</output>
